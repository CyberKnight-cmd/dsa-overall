#include <stdio.h>
#include <limits.h>

int main() {
    // printf("int: %d to %d\n", INT_MIN, INT_MAX);
    // printf("short: %d to %d\n", SHRT_MIN, SHRT_MAX);
    // printf("long long: %lld to %lld\n", LLONG_MIN, LLONG_MAX);
    
    
    // int x = 5;
    // int y = ++x * x++;
    // printf("%d", y);

    int x;
    x = (5, 6);
    printf("%d", x);
    return 0;
}
